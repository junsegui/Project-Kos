const INITIAL_STATE = {
  visible: false,
};

export const shopDesktopReducer = (state = INITIAL_STATE, action) => {
  const { type, payload } = action;
  switch (type) {
    default:
      return state;
  }
};
